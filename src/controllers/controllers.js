const uploadImage = require('./uploadImage');
const deleteImageById = require('./deleteImageById');

module.exports = {
    // getLsitOfImages: (req, res) => {},
    // downloadImageById: (req, res) => {},
    // mergeImages: (req, res) => {},
    uploadImage,
    deleteImageById
};